import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.tsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
/**
* COMMEND TESTING
*   Regular Comment
* * Important with asterisk
* ! Alert with exclamation
* ? Question with question mark
* TODO: Todo with TODO keyword
* @param Mention with @
//// this.lineOfCode() == commentedOut; with 4 slashes
*/
